<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Saldo Añadido</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .transaction-container {
            max-width: 400px;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            background-color: #fff;
            text-align: center;
        }

        h2 {
            color: #17a2b8;
        }

        p {
            font-size: 16px;
            margin: 8px 0;
        }

        .success {
            color: #0c5460;
            background-color: #d1ecf1;
            padding: 10px;
            border-radius: 5px;
            margin-top: 10px;
        }

        .btn {
            display: inline-block;
            padding: 10px 15px;
            margin-top: 15px;
            background: #17a2b8;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: 0.3s;
        }

        .btn:hover {
            background: #138496;
        }
    </style>
</head>
<body>

    <div class="transaction-container">
        <h2>💳 ¡Saldo Añadido!</h2>
        <p><strong>Monto Añadido:</strong> ${{amount}}</p>
        <p><strong>Método de Pago:</strong> {{paymentMethod}}</p>
        <p><strong>Fecha:</strong> {{date}}</p>

        <p class="success">✅ Tu saldo ha sido actualizado con éxito.</p>

        <a href="/wallet" class="btn">Ver mi Billetera</a>
    </div>

</body>
</html>
